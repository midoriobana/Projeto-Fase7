<!DOCTYPE html>
<html>
  <head>
    <title>ClassGeek</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
      
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
      
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      
      
      <!-- Navbars -->
<nav class="navbar navbar-dark bg-dark navbar navbar-expand-lg">
    <div class="container">
        
        <%= link_to "ClassGeek", root_path, class: "navbar-brand" %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item active"><%= link_to "Home", root_path, class: "nav-link" %></li>
            <li class="nav-item active"><%= link_to "Turmas", turmas_path, class: "nav-link" %></li> 
            <li class="nav-item active"><%= link_to "Tweets", tweets_path, class: "nav-link" %></li>
        </ul>
        
        <ul class="navbar-nav position-absolute end-0">
            
            <%if user_signed_in? %>
            
            <li class="nav-item mx-2"><%= link_to "Sair", destroy_user_session_path, method: :delete, class: "btn btn-danger" %></li>
               
            <% else %>  
            
            <li class="nav-item"><%= link_to "Entrar", new_user_session_path, class: "btn btn-success" %></li>
            <li class="nav-item mx-2"><%= link_to "Cadastrar", new_user_registration_path, class: "btn btn-primary" %></li>
            
            <% end %>
        </ul>
    </div>
    </div>
</nav>
      <% flash.each do |name, message| %>
        <%= content_tag(:div, message, class: "alert alert-info #{name}")%>
      <% end %>
      

      
    <%= yield %>
  </body>
</html>
